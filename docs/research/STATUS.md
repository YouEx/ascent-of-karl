# Superset-status

*Auto-genereret af `tools/superset_status.py` — redigér ikke i hånden.*

Supersettet (`superset.csv`) indeholder **14913 opskrifter**
fordelt på **13609 unikke ingredienspar** (samme par giver
forskellige resultater i forskellige spil) fra Little Alchemy 1+2,
Infinite Craft og 7 alchemy-kloner (se `README.md` for metodologi).

## Vores adoption

| Kategori | Antal | Andel af vores 205 kombinationer |
|---|---|---|
| Ingredienspar genfindes i supersettet | 24 | 12% |
| Resultatet findes (via anden opskrift) | 68 | 33% |
| Helt egne opfindelser | 113 | 55% |

## Ingredienspar genfundet i supersettet

- `stone + stone = sparks` — matcher `boulder` (infinite-craft)
- `stone + stick = stone axe` — matcher `hammer` (clones)
- `fire + meat = roast meat` — matcher `barbeque` (clones)
- `mud + mud = mud bath` — matcher `clay` (infinite-craft)
- `clay + fire = pottery` — matcher `brick` (clones;little-alchemy)
- `fire + water = steam` — matcher `alcohol` (clones)
- `ore + fire = copper` — matcher `metal` (little-alchemy)
- `stone + water = round stone` — matcher `earth` (clones)
- `stone + mud = standing stone` — matcher `clay` (little-alchemy)
- `hut + fire = burnt-down hut` — matcher `ash` (clones)
- `meat + smoke = smoked meat` — matcher `ham` (little-alchemy)
- `bird + fire = phoenix?` — matcher `phoenix` (clones;little-alchemy)
- `mud + fire = pottery` — matcher `brick` (infinite-craft;little-alchemy)
- `wheel + wheel = cart` — matcher `bicycle` (little-alchemy)
- `fire + fire = smoke` — matcher `energy` (little-alchemy)
- `cloud + cloud = rain` — matcher `storm` (little-alchemy)
- `snow + snow = ice` — matcher `blizzard` (little-alchemy)
- `net + fish = great catch` — matcher `catch` (infinite-craft)
- `rope + stick = snare` — matcher `bow` (clones)
- `bone + shrine = burial` — matcher `worship stones` (clones)
- `stone + bone = fossil` — matcher `fossil` (little-alchemy)
- `flour + water = dough` — matcher `dough` (clones;little-alchemy)
- `dough + fire = bread` — matcher `bread` (clones;little-alchemy)
- `dog + dog = hunting party` — matcher `puppy love` (clones)

## Resultater der findes i supersettet med andre opskrifter

- `sparks + dry grass = fire` — resultatet findes i supersettet (anden opskrift)
- `stone axe + stick = spear` — resultatet findes i supersettet (anden opskrift)
- `spear + wild boar = meat` — resultatet findes i supersettet (anden opskrift)
- `clay + water = mud` — resultatet findes i supersettet (anden opskrift)
- `stone axe + stone = ore` — resultatet findes i supersettet (anden opskrift)
- `copper + ore = bronze` — resultatet findes i supersettet (anden opskrift)
- `round stone + stone axe = wheel` — resultatet findes i supersettet (anden opskrift)
- `wheel + planks = cart` — resultatet findes i supersettet (anden opskrift)
- `water + spear = fish` — resultatet findes i supersettet (anden opskrift)
- `log + stick = drum` — resultatet findes i supersettet (anden opskrift)
- `mud brick + mud brick = hut` — resultatet findes i supersettet (anden opskrift)
- `hut + neighbour = village` — resultatet findes i supersettet (anden opskrift)
- `log + water = raft` — resultatet findes i supersettet (anden opskrift)
- `fire + dry grass = smoke` — resultatet findes i supersettet (anden opskrift)
- `fire + log = charcoal` — resultatet findes i supersettet (anden opskrift)
- `wild boar + stone axe = bone` — resultatet findes i supersettet (anden opskrift)
- `bone + stick = club` — resultatet findes i supersettet (anden opskrift)
- `bird + boomerang = feather` — resultatet findes i supersettet (anden opskrift)
- `footmobile + hut = garage` — resultatet findes i supersettet (anden opskrift)
- `planks + log = sledge` — resultatet findes i supersettet (anden opskrift)
- `cave brew + cave brew = hangover` — resultatet findes i supersettet (anden opskrift)
- `sparks + stick = fire` — resultatet findes i supersettet (anden opskrift)
- `round stone + stick = wheel` — resultatet findes i supersettet (anden opskrift)
- `hide cloak + pottery = drum` — resultatet findes i supersettet (anden opskrift)
- `raft + spear = fish` — resultatet findes i supersettet (anden opskrift)
- `fire + planks = charcoal` — resultatet findes i supersettet (anden opskrift)
- `roast meat + neighbour = bone` — resultatet findes i supersettet (anden opskrift)
- `bird + neighbour = feather` — resultatet findes i supersettet (anden opskrift)
- `feather + feather = wings` — resultatet findes i supersettet (anden opskrift)
- `steam + steam = cloud` — resultatet findes i supersettet (anden opskrift)
- `rain + standing stone = lightning` — resultatet findes i supersettet (anden opskrift)
- `lightning + dry grass = fire` — resultatet findes i supersettet (anden opskrift)
- `cloud + feather = snow` — resultatet findes i supersettet (anden opskrift)
- `snow + planks = skis` — resultatet findes i supersettet (anden opskrift)
- `dry grass + dry grass = rope` — resultatet findes i supersettet (anden opskrift)
- `rope + rope = net` — resultatet findes i supersettet (anden opskrift)
- `snare + meat = wolf` — resultatet findes i supersettet (anden opskrift)
- `wolf + roast meat = dog` — resultatet findes i supersettet (anden opskrift)
- `rope + stone axe = bow` — resultatet findes i supersettet (anden opskrift)
- `bow + feather = arrow` — resultatet findes i supersettet (anden opskrift)
- `arrow + wild boar = meat` — resultatet findes i supersettet (anden opskrift)
- `romance + hut = family` — resultatet findes i supersettet (anden opskrift)
- `neighbour + pottery = gift` — resultatet findes i supersettet (anden opskrift)
- `fish + round stone = shell` — resultatet findes i supersettet (anden opskrift)
- `shaman ugh + hide cloak = mask` — resultatet findes i supersettet (anden opskrift)
- `water + pottery = reflection` — resultatet findes i supersettet (anden opskrift)
- `berries + berries = seeds` — resultatet findes i supersettet (anden opskrift)
- `seeds + mud = garden` — resultatet findes i supersettet (anden opskrift)
- `garden + rain = harvest` — resultatet findes i supersettet (anden opskrift)
- `skis + snow = sledge` — resultatet findes i supersettet (anden opskrift)
- `rope + log = raft` — resultatet findes i supersettet (anden opskrift)
- `log + ice = amber` — resultatet findes i supersettet (anden opskrift)
- `acorn + dog = snare` — resultatet findes i supersettet (anden opskrift)
- `wild boar + snow = mammoth` — resultatet findes i supersettet (anden opskrift)
- `mammoth + hunting party = meat` — resultatet findes i supersettet (anden opskrift)
- `harvest + round stone = flour` — resultatet findes i supersettet (anden opskrift)
- `flour + pottery = dough` — resultatet findes i supersettet (anden opskrift)
- `cave painting + hide cloak = map` — resultatet findes i supersettet (anden opskrift)
- `stone circle + sun god = calendar` — resultatet findes i supersettet (anden opskrift)
- `calendar + harvest = bread` — resultatet findes i supersettet (anden opskrift)
- `net + bird = feather` — resultatet findes i supersettet (anden opskrift)
- `ice + pottery = reflection` — resultatet findes i supersettet (anden opskrift)
- `seeds + harvest = garden` — resultatet findes i supersettet (anden opskrift)
- `snow + hut = ice` — resultatet findes i supersettet (anden opskrift)
- `lightning + log = charcoal` — resultatet findes i supersettet (anden opskrift)
- `rain + seeds = garden` — resultatet findes i supersettet (anden opskrift)
- `necklace + romance = family` — resultatet findes i supersettet (anden opskrift)
- `tally stick + stone = calendar` — resultatet findes i supersettet (anden opskrift)

## Egne opfindelser (ikke i supersettet)

- `grubs + fire = roasted grubs`
- `grubs + clay = grub farm`
- `log + stone axe = planks`
- `planks + planks = crafting table`
- `cart + neighbour = footmobile`
- `standing stone + standing stone = stone circle`
- `wild boar + berries = pet boar`
- `meat + stone axe = hide cloak`
- `berries + stone = cave painting`
- `drum + neighbour = rock band`
- `berries + pottery = cave brew`
- `cave brew + neighbour = stone age party`
- `pottery + cave painting = pot friend`
- `stick + stick = boomerang`
- `grub farm + neighbour = grub stand`
- `mud + dry grass = mud brick`
- `raft + stone axe = canoe`
- `smoke + neighbour = smoke signals`
- `cave painting + standing stone = shrine`
- `neighbour + shrine = shaman ugh`
- `shaman ugh + cave brew = visions`
- `berries + water = berry juice`
- `mud + berries = mud pie`
- `fish + roast meat = surf 'n' turf`
- `club + neighbour = first argument`
- `bone + standing stone = bone toss`
- `bone + bird = bone flute`
- `hide cloak + berries = dyed hide`
- `feather + hide cloak = feather cloak`
- `feather cloak + neighbour = fashion show`
- `clay + neighbour = clay figurine`
- `cave painting + neighbour = handprint wall`
- `rock band + stone circle = stonehenge gig`
- `drum + drum = drum solo`
- `cave painting + cave painting = cave gallery`
- `footmobile + cave painting = drive-in`
- `footmobile + footmobile = traffic jam`
- `round stone + round stone = boules`
- `neighbour + neighbour = wrestling match`
- `charcoal + stone = cave painting`
- `berry juice + pottery = cave brew`
- `stone + neighbour = standing stone`
- `berries + round stone = berry juice`
- `bone + berry juice = herbal remedy`
- `village + feather cloak = coronation`
- `stonehenge gig + drum solo = the legend`
- `visions + cave painting = into the painting`
- `wings + standing stone = the flight`
- `first argument + club = the war`
- `herbal remedy + neighbour = the healing`
- `mud pie + cave brew = the last supper`
- `ice + snow = the long winter`
- `stone age party + feather cloak = romance`
- `family + village = tribe`
- `burial + cave painting = legacy`
- `pottery + gift = trade`
- `shell + trade = shell money`
- `shell money + village = market`
- `tribe + club = chief`
- `chief + standing stone = the first law`
- `shaman ugh + drum = ritual`
- `visions + burial = ancestor spirit`
- `ancestor spirit + cave painting = myth`
- `myth + stone circle = sun god`
- `mask + ritual = ritual dance`
- `sun god + ritual dance = the living god`
- `bone + feather = bone needle`
- `bone needle + hide cloak = sewn clothes`
- `bone needle + charcoal = tattoo`
- `reflection + stone axe = haircut`
- `bone + rope = splint`
- `dog + snare = great catch`
- `sewn clothes + feather = feather cloak`
- `lightning + stone = sparks`
- `dog + neighbour = hunting party`
- `amber + grubs = insect in amber`
- `fossil + myth = lake monster`
- `fossil + mammoth = mammoth-bone hut`
- `standing stone + ice = the monolith`
- `the monolith + ancestor spirit = the visitors`
- `the visitors + chief = golden idol`
- `berries + log = acorn`
- `mammoth + bone = mammoth-bone hut`
- `bread + cave brew = the tavern`
- `rain + village = the flood`
- `the flood + myth = the sunken village`
- `water + ancestor spirit = lake monster`
- `wolf + visions = the hairy one`
- `wrestling match + drum = training montage`
- `training montage + standing stone = the champion`
- `the champion + feather cloak = coronation`
- `shrine + pottery = the sacred pot`
- `the sacred pot + snare = booby trap`
- `golden idol + shrine = ritual`
- `bone + bone needle = tally stick`
- `tally stick + trade = accounts`
- `myth + symbol = writing`
- `cave painting + tally stick = symbol`
- `canoe + pot friend = the voyage`
- `map + canoe = the voyage`
- `dog + wild boar = pet boar`
- `bow + neighbour = hunting party`
- `writing + the first law = accounts`
- `bread + smoked meat = surf 'n' turf`
- `the tavern + neighbour = stone age party`
- `market + gift = shell money`
- `ritual dance + stone age party = ritual`
- `mask + cave painting = golden idol`
- `great catch + fire = smoked meat`
- `shell + bone needle = necklace`
- `mammoth + hide cloak = sewn clothes`
- `amber + necklace = shell money`
- `calendar + shaman ugh = myth`
